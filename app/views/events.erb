<h1 class="text-center">Statistics for Event Name: <br></h1>
  <h3 class="text-center"><%= @event_name %></h3>

<div class="row well">
  <p><strong>This is the hourly breakdown of requests received for the event name <%= @event_name %>.</strong></p>
  <% @hours.each_with_index do |num, index|%>
  <% if num != 0 %>
  <p><%= "For hour #{index}, there were #{num} requests received." %></p>
  <% end %>
  <% end %>
</div>
